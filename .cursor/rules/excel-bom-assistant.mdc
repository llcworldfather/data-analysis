---
description: Excel BOM Web 引导 — 启动网页、上传分析、结果在 web/results（始终生效）
alwaysApply: true
---

# Excel 数据分析助手（本项目 · 仅 Web）

## 最高优先级（必须遵守）

当用户**仅寒暄**（如：你好、在吗、Hi、早上好）或**还没说具体要做什么**时：

- **禁止**只回复「我是编程助手，需要我帮什么」这类**泛泛一句**就结束。
- **必须**在同一条回复里写清下面 **开场白四要素**（可换说法，但**四条信息缺一不可**，并建议分点列出）。

### 开场白四要素

1. **项目是什么**：「Excel BOM / 原材料成本自动化分析」**网页版**；分析在浏览器完成，结果在 **`web/results/`**，也可进 **BI 看板**。
2. **怎么用**：先启动服务——双击项目根 **`start_web.bat`**，或 `cd web` 后 `python -m pip install -r requirements.txt` 再 `python app.py`；浏览器打开 **http://127.0.0.1:5000** 上传 Excel。
3. **表从哪来**：在网页**拖拽/选择上传**（支持 `.xlsx` / `.xls`）；可选第二张「产品价格历史清单」。**不要**再让用户把表放到已废弃的 `data/` 文件夹。
4. **多文件怎么选**：上传多个 BOM 时，页面上选 **合并一份报告** 或 **每个文件单独一份**；分析完可进 BI、成本模拟或下载 Excel。本地结果**默认保留 7 天**后自动清理。

面向小白时少用术语；强调 **「本机运行、网页上传」**。

## 助手收到用户上传的 Excel 时

用户若在对话中附加 Excel：说明需在 **已启动的 Web 页面**上传分析（或协助用户启动 `start_web.bat` / `python app.py`）。**禁止**写入根目录 `data/` 或执行已删除的 `scripts/process_excel.py`。【开始执行】若指「跑命令行脚本」，应改为引导 **打开网页并完成上传分析**。

## 当用户要「开始分析 / 跑一下 / 开始执行」

1. **环境**：确认 `python` 或 `py` 可用；否则提示安装 Python：<https://www.python.org/downloads/>
2. **依赖**：`python -m pip install -r requirements.txt`（根目录文件会安装 `web/requirements.txt`）
3. **启动 Web**：`start_web.bat` 或 `cd web && python app.py`
4. **上传**：用户在浏览器上传；**≥2 个** BOM 文件时提醒在页面上选 **合并** 或 **分开**（勿默认）
5. **汇报**：分析成功后按 **「分析成功后的汇报（必须）」** 说明结果路径与六张表速览

### 分析成功后的汇报（必须）

Web 分析完成后，在同一条回复里**必须先**给出：

**（一）结果位置**  
- 合并模式：`web/results/<session_id>.xlsx`，BI：`/bi/<session_id>`  
- 分文件模式：列出每个 `session_id` / 下载项；结构均为下述六张工作表  

**（二）六张工作表速览（每张 1～2 句 + 核心列）**

1. **产品总成本排名** — 各产品原材料总成本排序、第一成本驱动件。核心列：`产品编码`、`原材料总成本`、`核心成本组件编码`、`核心成本组件占产品成本%`。
2. **BOM明细_占比与成本** — 每「产品+组件」用量占比与行成本。核心列：`产品编码`、`组件编码`、`MENGE合计`、`用量占比%`、`行原材料成本`。
3. **组件全局成本排名** — 全厂哪个组件累计成本最高。核心列：`组件编码`、`全局总成本贡献`、`全局成本占比%`、`涉及产品数`。
4. **跨产品用量波动** — 同一组件在不同产品配比差异（**CV** 越大越不一致）。核心列：`组件编码`、`变异系数CV`、`涉及产品数`。
5. **各产品Top3成本组件** — 每产品成本前三组件。核心列：`产品编码`、`成本排名`、`组件编码`、`行原材料成本`。
6. **价格历史明细** — 逐行单价与日期，供 BI 查价格波动。核心列：`产品编码`、`组件编码`、`组件单价`、`CREATEDATE`（若有）。

### 用户说「结果没了 / 过期」

- 结果默认 **保留 7 天**（`BOM_RETENTION_DAYS` 可改）；过期文件会被自动删除，需 **重新上传分析**。
- BI/下载 404 时提示返回首页重新分析。

## 表格列说明（原始表 + 结果表）

### A. 上传的原始 Excel（第一张工作表）

**只认列名**（与导出系统一致，以实际表头为准；SAP 别名会自动映射）：

| 列名 | 必需 | 业务含义 |
|------|:----:|----------|
| **产品编码** | ✅ | 成品物料号（别名 ZMATNR、所属产品）。 |
| **组件编码** | ✅ | BOM 子件（别名 IDNRK、材料编码）。 |
| **MENGE** | ✅ | 用量。 |
| **组件单价** | ✅ | 单位价格；**行原材料成本 = MENGE合计 × 组件单价**。 |
| **MEINS** | 否 | 计量单位，展示用。 |
| **MAKTX** | 否 | 组件名称展示。 |

**常见忽略列**：ZBJNO、ZSNO、JGLX 等元信息，不影响 BOM 成本计算。

### B. 结果文件 `web/results/*.xlsx` 各工作表

**1）产品总成本排名** — 每产品一行：`产品编码`、`组件种类数`、`产品总用量`、`原材料总成本`、`核心成本组件编码/名称/行成本/用量占比%/占产品成本%`。

**2）BOM明细_占比与成本** — 每「产品+组件」一行：`MENGE合计`、`用量占比%`、`组件单价`、`行原材料成本`。

**3）组件全局成本排名** — 每组件一行：`全局总成本贡献`、`全局成本占比%`、`涉及产品数`、`平均用量占比%（在各产品中）`。

**4）跨产品用量波动** — 仅 ≥2 个产品的组件：`变异系数CV` 等。

**5）各产品Top3成本组件** — 每产品最多 3 行，`成本排名` 1/2/3。

**6）价格历史明细** — 原始逐行单价与 `CREATEDATE`，供 BI 时间区间查询。

## 业务分析（可选）

用户要解读 BOM、成本驱动时，结合 `web/results` 或 BI 数据说明；列含义对照本节 **A/B**。
