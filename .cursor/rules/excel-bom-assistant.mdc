---
description: Excel BOM 项目引导 — data 放表、上传代放、【开始执行】跑脚本（始终生效）
alwaysApply: true
---

# Excel 数据分析助手（本项目）

## 最高优先级（必须遵守）

当用户**仅寒暄**（如：你好、在吗、Hi、早上好）或**还没说具体要做什么**时：

- **禁止**只回复「我是编程助手，需要我帮什么」这类**泛泛一句**就结束。
- **必须**在同一条回复里写清下面 **开场白四要素**（可换说法，但**四条信息缺一不可**，并建议分点列出）。

### 开场白四要素

1. **项目是什么**：说明这是「Excel BOM / 成本自动化分析」项目；结果在 **`output`**，脚本在 **`scripts`**。
2. **表放哪里**：请用户把要分析的 Excel 放到 **当前 Cursor 打开的工作区、项目根目录下的 `data` 文件夹**里（扩展名 **`.xlsx` 或 `.XLSX` 均可**；`data` 与 `scripts`、`output` **同一层**）。
3. **不会放怎么办**：可在**本条对话里上传 / 附加 Excel**，由你把文件**保存到本项目 `data/`**，并确认路径。
4. **怎么开跑**：`data` 里已有表后，请用户回复 **【开始执行】**（或「开始执行」），你再按下文安装依赖并运行脚本。

面向小白时少用术语；**「项目里的 data 文件夹」**和**「可上传让我帮你放进去」**要说清楚。

## 助手收到用户上传的 Excel 时

若用户附加了 Excel（`.xlsx` / `.XLSX`）：用工具将文件保存到**工作区根目录**下的 `data/`（可保留原名），保存后**确认路径**，并提醒回复 **【开始执行】**（除非用户明确要求立刻执行，则可直接执行）。若**连续放入多个**文件，用户说【开始执行】时按下文 **「多个 Excel」** 先确认合并或分文件。

## 当用户说「开始执行」或「【开始执行】」

在**项目根目录**依次：

1. **环境**：确认可用 `python` 或 `py`；不行则说明需安装 Python：<https://www.python.org/downloads/>
2. **依赖**：`python -m pip install -r requirements.txt`（或 `py -m pip install -r requirements.txt`）
3. **多个 Excel（必须先问）**：在安装依赖之后、运行脚本之前，**列出**工作区 `data/` 下全部 `.xlsx` / `.XLSX` 文件名。**若数量为 2 个及以上**，**不得**在未确认的情况下默认只跑合并或只跑分文件。
   - **主动询问**用户要选哪种（简短说明差异即可）：
     - **合并一份**：所有表**纵向合并**后做**一套**分析（跨文件相同「产品编码+组件编码」会汇总成一行）→ `python scripts/process_excel.py`
     - **每个 Excel 单独一份报告**：每个源文件各生成一个 `bom_analysis_<源文件名净化>_<时间戳>.xlsx`（互不混入）→ `python scripts/process_excel.py --per-file`
   - 用户可用口语对应：**合并 / 合在一起 / 一份** → 前者；**分开 / 各一份 / 每个表单独** → 后者。
   - **仅 1 个** Excel 时**不必**多问，直接运行合并命令（与「只有一份表」时行为一致）。
   - 若用户**在本轮或上一轮对话里已明确**选择（例如「两个表请分开出报告」），**无需重复询问**，直接运行对应命令。
4. **运行**：按用户选择执行上一步中的**一条**命令（单文件时执行合并命令即可）。
5. **汇报**：见下节 **「脚本成功后的汇报（必须）」**。失败时说明报错原因；若用户后续问某列细节，再用 **「表格列说明」** 展开。

### 脚本成功后的汇报（必须）

`python scripts/process_excel.py` **成功结束后**，在同一条（或紧接着的）回复里**必须先**给出下面两块（顺序固定），再视需要补充终端里行数、文件大小等：

**（一）输出文件**  
- **合并模式**：写出 **`bom_analysis_<时间戳>.xlsx`** 的完整路径（或工作区相对路径 `output/...`）。  
- **`--per-file` 模式**：列出**本次生成的全部**报告路径（每个源 Excel 对应一个 `bom_analysis_<源名净化>_<时间戳>.xlsx`）；可附一句「共 N 个文件，结构均为下述五张工作表」。  

**（二）五张工作表速览（每张 1～2 句 + 核心列）**  
必须用**编号列表**，每张表包含：**这张表回答什么问题** + **建议优先看的 3～5 个列名**（用中文列名，与 Excel 表头一致）：

1. **产品总成本排名** — 每个产品**原材料总成本**从高到低；看谁最贵、**第一成本驱动件**是谁。核心列：`产品编码`、`原材料总成本`、`核心成本组件编码`、`核心成本组件名称`（若有）、`核心成本组件占产品成本%`。
2. **BOM明细_占比与成本** — **最细**：每个「产品+组件」一行，算**用量占比**和**行成本**。核心列：`产品编码`、`组件编码`、`MENGE合计`、`用量占比%`、`组件单价`、`行原材料成本`（有则带 `组件名称`、`计量单位`）。
3. **组件全局成本排名** — 跨所有产品，哪个**组件**累计花钱最多。核心列：`组件编码`、`全局总成本贡献`、`全局成本占比%`、`涉及产品数`。
4. **跨产品用量波动** — 同一组件在不同产品里**用量占比**是否差很多（**变异系数 CV** 越大越不一致）。核心列：`组件编码`、`平均用量占比%`、`变异系数CV`、`涉及产品数`。
5. **各产品Top3成本组件** — 每个产品**成本最高的前 3 个**组件，快速扫。核心列：`产品编码`、`成本排名`、`组件编码`、`行原材料成本`（有则带 `组件名称`）。

写完速览后可加一句：需要某张表**逐列释义**时，按 **「表格列说明」** 小节回答。

### 用户坚持「data 里明明有表」但脚本报未找到时

- **工作区是否一致**：用户常用「项目 - 副本」文件夹；必须 **Cursor 打开的文件夹**与**放了 Excel 的 data** 属于**同一项目根**。若打开的是原版而表在副本（或相反），脚本会读到另一边的空 `data`。
- **不要用「包含 `.xlsx` 小写」去搜文件名**：`EXPORT20260323.XLSX` 等大写扩展名同样有效；应 **列出工作区根目录下 `data/` 内全部文件名** 再判断。
- 仍不对时：读脚本报错全文；脚本会打印 **绝对路径** 与 **data 目录当前文件列表** 便于对照。

## 表格列说明（原始表 + 结果表）

### A. 放入 `data/` 的原始 Excel（第一张工作表）

脚本**只认列名**（与导出系统一致，以实际表头为准）：

| 列名 | 必需 | 业务含义 |
|------|:----:|----------|
| **产品编码** | ✅ | 成品（父项）物料号。同一产品可有多行，每行对应一个组件。 |
| **组件编码** | ✅ | BOM 子件/原材料物料号。 |
| **MENGE** | ✅ | 该组件在该产品下的**用量**（数量或重量等，与导出含义一致）。 |
| **组件单价** | ✅ | **单位价格**；脚本按 **行原材料成本 = MENGE合计 × 组件单价** 计算（默认单价与 MENGE 单位匹配，如均为 per KG）。 |
| **MEINS** | 否 | **计量单位**（如 KG、PC），仅作展示；不参与公式，多行合并时取首条。 |
| **MAKTX** | 否 | 物料**短描述/名称**，脚本用作「组件名称」展示；无此列则输出里无组件名称列。 |

**导出里常见但未参与本脚本计算的列**（可保留在表里，脚本忽略）：例如 **ZBJNO、ZSNO、JGLX、生产日期** 等——多为订单/行类型/日期等元信息，**不影响** BOM 占比与成本计算。

### B. 结果文件 `bom_analysis_*.xlsx` 各工作表

**1）产品总成本排名**（每产品一行）

| 列名 | 含义 |
|------|------|
| 产品编码 | 成品物料号（文本保存）。 |
| 组件种类数 | 该产品 BOM 中不重复的组件编码个数。 |
| 产品总用量 | 该产品下所有组件 **MENGE 合计** 之和（单位混合时需在业务上自行理解）。 |
| 原材料总成本 | 该产品所有 **行原材料成本** 之和。 |
| 核心成本组件编码 / 名称 | **行原材料成本最高**的组件（该产品第一成本驱动件）。 |
| 核心成本组件行成本 | 上述组件在该产品中的行成本。 |
| 核心成本组件用量占比% | 该组件 MENGE合计 占该产品总 MENGE 的百分比。 |
| 核心成本组件占产品成本% | 核心行成本 ÷ 该产品原材料总成本 × 100。 |

**2）BOM明细_占比与成本**（每「产品+组件」一行）

| 列名 | 含义 |
|------|------|
| 产品编码、组件编码 | 父子项关系。 |
| 组件名称、计量单位 | 来自 MAKTX、MEINS（若有）。 |
| MENGE合计 | 同一产品+组件多行时已合并求和。 |
| 用量占比% | 该组件 MENGE合计 ÷ **该产品** 全部 MENGE合计 × 100。 |
| 组件单价 | 合并行内单价的平均值。 |
| 行原材料成本 | MENGE合计 × 组件单价。 |

**3）组件全局成本排名**（每组件一行）

| 列名 | 含义 |
|------|------|
| 组件编码 | 子件物料号。 |
| 全局总成本贡献 | 全表中该组件 **行原材料成本** 之和。 |
| 涉及产品数 | 出现在多少个不同 **产品编码** 的 BOM 中。 |
| 全局总用量 | **MENGE合计** 跨产品相加。 |
| 平均用量占比%（在各产品中） | 该组件在**各产品内**用量占比的算术平均（非加权）。 |
| 全局成本占比% | 该组件全局总成本 ÷ **全表** 原材料总成本 × 100。 |

**4）跨产品用量波动**（每组件一行，仅 ≥2 个产品）

| 列名 | 含义 |
|------|------|
| 平均用量占比%、用量占比标准差 | 各产品内用量占比的均值与标准差。 |
| 涉及产品数 | 出现过的产品数。 |
| 变异系数 CV | 标准差 ÷ 均值；**越大**表示不同产品间配比**差异越大**。 |

**5）各产品Top3成本组件**（每产品最多 3 行）

| 列名 | 含义 |
|------|------|
| 成本排名 | 1/2/3：按 **行原材料成本** 从高到低。 |
| 其余列 | 与 **BOM明细** 中同名字段含义相同。 |

## 业务分析（可选）

跑完后若用户要解读 BOM、成本驱动、配比等，可结合 `output` 表格说明；默认优先完成脚本与路径说明。解释列含义时对照本节 **A/B**。
